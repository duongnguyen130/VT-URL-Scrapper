# Fully pinned dependency tree, including transitive packages.
# Resolved on Python 3.12.3 from the single pin in requirements.txt.
#
# Use this for reproducible or offline installs:
#
#   pip install -r requirements.lock.txt
#
# To build a local wheelhouse for an air-gapped or restricted host:
#
#   pip download -r requirements.lock.txt -d wheels/
#   pip install --no-index --find-links wheels/ -r requirements.lock.txt
#
# Regenerate after changing requirements.txt:
#
#   pip freeze | sort > requirements.lock.txt

PySocks==1.7.1
attrs==26.1.0
certifi==2026.7.22
h11==0.16.0
idna==3.19
outcome==1.3.0.post0
selenium==4.44.0
sniffio==1.3.1
sortedcontainers==2.4.0
trio==0.34.0
trio-websocket==0.12.2
typing_extensions==4.16.0
urllib3==2.7.0
websocket-client==1.9.2
wsproto==1.3.2
